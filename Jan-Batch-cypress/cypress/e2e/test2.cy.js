describe('template spec', () => {
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
  it('passes', () => {
    cy.visit('https://example.cypress.io')
  })
})

// describe() // this is method declaration in JS 
//describe('Name',()=> {}) => 
  //describe('NAme of the test suite',function() {})
  //1. Name of the test suite - it is a string ,
  //2. function () or arrow function ()=>
  //3. Block scope {} => defining the scope of your describe or test suite 

  // it() syntax 
  //it('Name of the test', ()=> {})
    //it('Name of the test', function() {})
    //1. Name of the test case , => it is a string 
    //2. function() or ()=> 
    //3. {} // scope of your test will be defined in between {}
    